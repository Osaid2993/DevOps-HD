{
  "name": "hd-jenkins-unique-ci",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo \"Nothing to build for Node\"",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --ci --coverage --reporters=default --reporters=jest-junit",
    "lint": "eslint ."
  },
  "jest-junit": {
    "outputDirectory": "reports/junit",
    "outputName": "junit-results.xml"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.1.1",
    "@stryker-mutator/jest-runner": "^9.1.1",
    "eslint": "^9.1.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.4"
  }
}
